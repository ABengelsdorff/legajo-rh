// import { contextBridge, ipcRenderer } from 'electron';

// // Exponer una API limitada al renderer
// contextBridge.exposeInMainWorld('api', {
//   getData: async () => ipcRenderer.invoke('get-data'),  // Invoca evento en el main
//   saveData: async (formData: any) => ipcRenderer.invoke('save-data', formData)
// });
